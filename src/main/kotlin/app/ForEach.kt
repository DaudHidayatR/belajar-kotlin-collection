package app

fun main(){
    listOf("Daud", "Hidayat", "Ramadhan").forEach { println(it) }
    setOf("Daud", "Hidayat", "Ramadhan").forEach { println(it) }
    mutableListOf("Daud", "Hidayat", "Ramadhan").forEach { println(it) }
}